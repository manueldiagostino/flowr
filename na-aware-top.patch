diff --git a/src/abstract-interpretation/data-frame/dataframe-domain.ts b/src/abstract-interpretation/data-frame/dataframe-domain.ts
index 5ae146bb..0b58c487 100644
--- a/src/abstract-interpretation/data-frame/dataframe-domain.ts
+++ b/src/abstract-interpretation/data-frame/dataframe-domain.ts
@@ -1,4 +1,4 @@
-import type { AbstractDomainValue } from '../domains/abstract-domain';
+import type { AbstractValue } from '../domains/abstract-domain';
 import { PosIntervalDomain } from '../domains/positive-interval-domain';
 import { ProductDomain } from '../domains/product-domain';
 import { SetRangeDomain } from '../domains/set-range-domain';
@@ -11,7 +11,7 @@ export type AbstractDataFrameShape = {
 };
 
 /** The type of abstract values of a sub abstract domain (shape property) of the data frame shape product domain */
-export type DataFrameShapeProperty<Property extends keyof AbstractDataFrameShape> = AbstractDomainValue<AbstractDataFrameShape[Property]>;
+export type DataFrameShapeProperty<Property extends keyof AbstractDataFrameShape> = AbstractValue<AbstractDataFrameShape[Property]>;
 
 /**
  * The data frame abstract domain as product domain of a column names domain, column count domain, and row count domain.
diff --git a/src/abstract-interpretation/domains/abstract-domain.ts b/src/abstract-interpretation/domains/abstract-domain.ts
index a1c7e633..cccd9b11 100644
--- a/src/abstract-interpretation/domains/abstract-domain.ts
+++ b/src/abstract-interpretation/domains/abstract-domain.ts
@@ -120,6 +120,13 @@ implements Lattice<Abstract, Top, Bot, Value> {
  */
 export type AnyAbstractDomain = AbstractDomain<unknown, unknown, unknown, unknown>;
 
+/**
+ * The type of the abstract values of an abstract domain (including the Top and Bottom element).
+ * @template Domain - The abstract domain to get the abstract value type for
+ */
+export type AbstractValue<Domain extends AnyAbstractDomain> =
+	Domain extends AbstractDomain<unknown, infer Value, infer Top, infer Bot> ? Value | Top | Bot : never;
+
 /**
  * The type of the concrete domain of an abstract domain.
  * @template Domain - The abstract domain to get the concrete domain type for
@@ -128,25 +135,25 @@ export type ConcreteDomain<Domain extends AnyAbstractDomain> =
 	Domain extends AbstractDomain<infer Concrete, unknown, unknown, unknown> ? Concrete : never;
 
 /**
- * The type of the abstract values of an abstract domain (including the Top and Bottom element).
- * @template Domain - The abstract domain to get the abstract value type for
+ * The type of an abstract domain holding an abstract value of the domain.
+ * @template Domain - The abstract domain abstract domain value type for
  */
 export type AbstractDomainValue<Domain extends AnyAbstractDomain> =
-	Domain extends AbstractDomain<unknown, infer Value, infer Top, infer Bot> ? Value | Top | Bot : never;
+	Domain extends AbstractDomain<infer Concrete, infer Value, infer Top, infer Bot> ? Domain & AbstractDomain<Concrete, Value, Top, Bot, Value> : never;
 
 /**
- * The type of the Top element (greatest element) of an abstract domain.
- * @template Domain - The abstract domain to get the Top element type for
+ * The type an abstract domain holding the Top element (greatest element) of the domain.
+ * @template Domain - The abstract domain to get the abstract domain top for
  */
 export type AbstractDomainTop<Domain extends AnyAbstractDomain> =
-	Domain extends AbstractDomain<unknown, unknown, infer Top, unknown> ? Top : never;
+	Domain extends AbstractDomain<infer Concrete, infer Value, infer Top, infer Bot> ? Domain & AbstractDomain<Concrete, Value, Top, Bot, Top> : never;
 
 /**
- * The type of the Bottom element (least element) of an abstract domain.
- * @template Domain - The abstract domain to get the Bottom element type for
+ * The type an abstract domain holding the Bottom element (least element) of the domain.
+ * @template Domain - The abstract domain to get the abstract domain bottom for
  */
 export type AbstractDomainBottom<Domain extends AnyAbstractDomain> =
-	Domain extends AbstractDomain<unknown, unknown, unknown, infer Bot> ? Bot : never;
+	Domain extends AbstractDomain<infer Concrete, infer Value, infer Top, infer Bot> ? Domain & AbstractDomain<Concrete, Value, Top, Bot, Bot> : never;
 
 /**
  * Converts an element of an abstract domain into a string.
diff --git a/src/abstract-interpretation/vector/na-aware-domain.ts b/src/abstract-interpretation/vector/na-aware-domain.ts
index f4f8e2c9..104b6beb 100644
--- a/src/abstract-interpretation/vector/na-aware-domain.ts
+++ b/src/abstract-interpretation/vector/na-aware-domain.ts
@@ -1,10 +1,10 @@
-/* eslint-disable @typescript-eslint/unified-signatures */
 import type {
+	AbstractDomainTop,
 	AnyAbstractDomain,
 	ConcreteDomain,
 } from '../domains/abstract-domain';
 import { AbstractDomain } from '../domains/abstract-domain';
-import { Top, Bottom, NA } from '../domains/lattice';
+import { Top, Bottom, NA, BottomSymbol, NASymbol } from '../domains/lattice';
 import type { DomainFactory } from './known-initial-positions-domain';
 
 /**
@@ -20,7 +20,12 @@ export type NAAwareInnerValue<Domain extends AnyAbstractDomain> = {
 /**
  * The Top element type for NAAwareDomain.
  */
-export type NAAwareTop = typeof Top;
+export type NAAwareTop<Domain extends AnyAbstractDomain> = {
+	/** The wrapped inner domain value */
+	readonly inner: Domain & AbstractDomainTop<Domain>;
+	/** Whether this abstract value contains NA */
+	readonly hasNA: true;
+};
 
 /**
  * The Bottom element type for NAAwareDomain.
@@ -32,7 +37,7 @@ export type NAAwareBottom = typeof Bottom;
  */
 export type NAAwareValue<Domain extends AnyAbstractDomain> =
   | NAAwareInnerValue<Domain>
-  | NAAwareTop
+  | NAAwareTop<Domain>
   | NAAwareBottom;
 
 /**
@@ -52,7 +57,7 @@ export class NAAwareDomain<
 	extends AbstractDomain<
 		ConcreteDomain<Domain> | typeof NA,
 		NAAwareInnerValue<Domain>,
-		NAAwareTop,
+		NAAwareTop<Domain>,
 		NAAwareBottom,
 		Value
 	> {
@@ -63,8 +68,9 @@ export class NAAwareDomain<
 		this.factory = factory;
 	}
 
-	public create(value: NAAwareValue<Domain>): this {
-		return new NAAwareDomain(value, this.factory) as this;
+	public create(value: NAAwareValue<Domain>): this;
+	public create(value: NAAwareValue<Domain>): NAAwareDomain<Domain> {
+		return new NAAwareDomain(value, this.factory);
 	}
 
 	/**
@@ -72,8 +78,8 @@ export class NAAwareDomain<
 	 */
 	public static top<Domain extends AnyAbstractDomain>(
 		factory: DomainFactory<Domain>
-	): NAAwareDomain<Domain, NAAwareTop> {
-		return new NAAwareDomain(Top, factory);
+	): NAAwareDomain<Domain, NAAwareTop<Domain>> {
+		return new NAAwareDomain({ inner: factory(Top) as AbstractDomainTop<Domain>, hasNA: true }, factory);
 	}
 
 	/**
@@ -96,8 +102,8 @@ export class NAAwareDomain<
 		return new NAAwareDomain({ inner: innerBottom, hasNA: true }, factory);
 	}
 
-	public top(): this & NAAwareDomain<Domain, NAAwareTop>;
-	public top(): NAAwareDomain<Domain, NAAwareTop> {
+	public top(): this & NAAwareDomain<Domain, NAAwareTop<Domain>>;
+	public top(): NAAwareDomain<Domain, NAAwareTop<Domain>> {
 		return NAAwareDomain.top(this.factory);
 	}
 
@@ -110,7 +116,7 @@ export class NAAwareDomain<
 	 * Checks if this abstract value IS exactly NA (pure NA, no other values).
 	 */
 	public isNA(): boolean {
-		if(this.value === Top || this.value === Bottom) {
+		if(this.value === Bottom) {
 			return false;
 		}
 		// Pure NA: inner domain is Bottom (no concrete values) but hasNA is true
@@ -121,10 +127,6 @@ export class NAAwareDomain<
 	 * Checks if this abstract value contains NA (may also contain other values).
 	 */
 	public containsNA(): boolean {
-		if(this.value === Top) {
-			// Top may contain NA (we don't know)
-			return true;
-		}
 		if(this.value === Bottom) {
 			return false;
 		}
@@ -136,35 +138,24 @@ export class NAAwareDomain<
 	 * Returns undefined if this is Top or Bottom.
 	 */
 	public getInner(): Domain | undefined {
-		if(this.value === Top || this.value === Bottom) {
+		if(this.value === Bottom) {
 			return undefined;
 		}
 		return this.value.inner;
 	}
 
-	public equals(other: NAAwareDomain<Domain>): boolean;
-	public equals(other: this): boolean;
-	public equals(other: this | NAAwareDomain<Domain>): boolean {
+	public equals(other: this): boolean {
 		if(this.value === other.value) {
 			return true;
 		}
-		if(this.isBottom() || other.isBottom()) {
-			return false;
-		}
-		if(this.isTop() || other.isTop()) {
-			return false;
-		}
-		if(this.value === Top || this.value === Bottom ||
-			other.value === Top || other.value === Bottom) {
+		if(this.value === Bottom || other.value === Bottom) {
 			return false;
 		}
 		return this.value.inner.equals(other.value.inner) &&
 			this.value.hasNA === other.value.hasNA;
 	}
 
-	public leq(other: NAAwareDomain<Domain>): boolean;
-	public leq(other: this): boolean;
-	public leq(other: this | NAAwareDomain<Domain>): boolean {
+	public leq(other: this): boolean {
 		if(this.equals(other)) {
 			return true;
 		}
@@ -177,8 +168,7 @@ export class NAAwareDomain<
 		if(other.isBottom() || this.isTop()) {
 			return false;
 		}
-		if(this.value === Top || this.value === Bottom ||
-			other.value === Top || other.value === Bottom) {
+		if(this.value === Bottom || other.value === Bottom) {
 			return false;
 		}
 		// Component-wise ordering
@@ -192,9 +182,7 @@ export class NAAwareDomain<
 	 * - valueWithNA.join(pureNA) = valueWithNA
 	 * - pureNA.join(pureNA) = pureNA
 	 */
-	public join(other: NAAwareDomain<Domain>): this;
-	public join(other: this): this;
-	public join(other: this | NAAwareDomain<Domain>): this {
+	public join(other: this): this {
 		if(this.isBottom()) {
 			return this.create(other.value);
 		}
@@ -224,9 +212,7 @@ export class NAAwareDomain<
 		return this.create({ inner: joinedInner, hasNA: mergedHasNA });
 	}
 
-	public meet(other: NAAwareDomain<Domain>): this;
-	public meet(other: this): this;
-	public meet(other: this | NAAwareDomain<Domain>): this {
+	public meet(other: this): this {
 		if(this.isBottom() || other.isBottom()) {
 			return this.bottom();
 		}
@@ -249,9 +235,7 @@ export class NAAwareDomain<
 		return this.create({ inner: metInner, hasNA: metHasNA });
 	}
 
-	public widen(other: NAAwareDomain<Domain>): this;
-	public widen(other: this): this;
-	public widen(other: this | NAAwareDomain<Domain>): this {
+	public widen(other: this): this {
 		if(this.isBottom()) {
 			return this.create(other.value);
 		}
@@ -274,9 +258,7 @@ export class NAAwareDomain<
 		return this.create({ inner: widenedInner, hasNA: mergedHasNA });
 	}
 
-	public narrow(other: NAAwareDomain<Domain>): this;
-	public narrow(other: this): this;
-	public narrow(other: this | NAAwareDomain<Domain>): this {
+	public narrow(other: this): this {
 		if(this.isBottom() || other.isBottom()) {
 			return this.bottom();
 		}
@@ -307,9 +289,6 @@ export class NAAwareDomain<
 	 * - Bottom → empty set
 	 */
 	public concretize(limit: number): ReadonlySet<ConcreteDomain<Domain> | typeof NA> | typeof Top {
-		if(this.value === Top) {
-			return Top;
-		}
 		if(this.value === Bottom) {
 			return new Set();
 		}
@@ -366,11 +345,8 @@ export class NAAwareDomain<
 	}
 
 	public toJson(): unknown {
-		if(this.value === Top) {
-			return '⊤';
-		}
 		if(this.value === Bottom) {
-			return '⊥';
+			return Bottom.description;
 		}
 		return {
 			inner: this.value.inner.toJson(),
@@ -379,55 +355,25 @@ export class NAAwareDomain<
 	}
 
 	public toString(): string {
-		if(this.value === Top) {
-			return '⊤';
-		}
 		if(this.value === Bottom) {
-			return '⊥';
-		}
-
-		if(this.isNA()) {
-			return 'NA';
+			return BottomSymbol;
+		} else if(this.isNA()) {
+			return NASymbol;
 		}
 		const innerStr = this.value.inner.toString();
-		const naMarker = this.value.hasNA ? '+NA' : '';
+		const naMarker = this.value.hasNA ? `+${NASymbol}` : '';
 		return `${innerStr}${naMarker}`;
 	}
 
-	public isTop(): this is AbstractDomain<
-		ConcreteDomain<Domain> | typeof NA,
-		NAAwareInnerValue<Domain>,
-		typeof Top,
-		typeof Bottom,
-		typeof Top
-	> {
-		return this.value === Top;
+	public isTop(): this is NAAwareDomain<Domain, NAAwareTop<Domain>> {
+		return this.value !== Bottom && this.value.inner.isTop() && this.value.hasNA;
 	}
 
-	public isBottom(): this is AbstractDomain<
-		ConcreteDomain<Domain> | typeof NA,
-		NAAwareInnerValue<Domain>,
-		typeof Top,
-		typeof Bottom,
-		typeof Bottom
-	> {
-		if(this.value === Bottom) {
-			return true;
-		}
-		if(this.value !== Top && this.value !== Bottom) {
-			// Lattice bottom: inner domain is bottom AND no NA flag
-			return this.value.inner.isBottom() && !this.value.hasNA;
-		}
-		return false;
+	public isBottom(): this is NAAwareDomain<Domain, NAAwareBottom> {
+		return this.value === Bottom || (this.value.inner.isBottom() && !this.value.hasNA);
 	}
 
-	public isValue(): this is AbstractDomain<
-		ConcreteDomain<Domain> | typeof NA,
-		NAAwareInnerValue<Domain>,
-		typeof Top,
-		typeof Bottom,
-		NAAwareInnerValue<Domain>
-	> {
-		return this.value !== Top && this.value !== Bottom;
+	public isValue(): this is NAAwareDomain<Domain, NAAwareInnerValue<Domain>> {
+		return this.value !== Bottom;
 	}
 }
diff --git a/test/functionality/abstract-interpretation/domains/domains.test.ts b/test/functionality/abstract-interpretation/domains/domains.test.ts
index 70128fb9..e7a6325c 100644
--- a/test/functionality/abstract-interpretation/domains/domains.test.ts
+++ b/test/functionality/abstract-interpretation/domains/domains.test.ts
@@ -1,5 +1,5 @@
 import { describe } from 'vitest';
-import type { AbstractDomainValue, AnyAbstractDomain } from '../../../../src/abstract-interpretation/domains/abstract-domain';
+import type { AbstractValue, AnyAbstractDomain } from '../../../../src/abstract-interpretation/domains/abstract-domain';
 import { BoundedSetDomain } from '../../../../src/abstract-interpretation/domains/bounded-set-domain';
 import { IntervalDomain, IntervalTop } from '../../../../src/abstract-interpretation/domains/interval-domain';
 import { Bottom, Top } from '../../../../src/abstract-interpretation/domains/lattice';
@@ -11,7 +11,7 @@ import { assertAbstractDomain } from './domain';
 
 describe('Abstract Domains', () => {
 	describe('Singleton Domain', () => {
-		const create = (value: AbstractDomainValue<SingletonDomain<number>>) => new SingletonDomain(value);
+		const create = (value: AbstractValue<SingletonDomain<number>>) => new SingletonDomain(value);
 
 		assertAbstractDomain(create, Bottom, Bottom, {
 			equal: true, leq: true, join: Bottom, meet: Bottom, widen: Bottom, narrow: Bottom, concrete: []
@@ -190,12 +190,12 @@ describe('Abstract Domains', () => {
 
 	const intervalTests = [{
 		name:   'Interval Domain',
-		create: (value: AbstractDomainValue<IntervalDomain>) => new IntervalDomain(value),
+		create: (value: AbstractValue<IntervalDomain>) => new IntervalDomain(value),
 		min:    IntervalTop[0],
 		max:    IntervalTop[1]
 	}, {
 		name:   'Positive Interval Domain',
-		create: (value: AbstractDomainValue<PosIntervalDomain>) => new PosIntervalDomain(value),
+		create: (value: AbstractValue<PosIntervalDomain>) => new PosIntervalDomain(value),
 		min:    PosIntervalTop[0],
 		max:    PosIntervalTop[1]
 	}];
